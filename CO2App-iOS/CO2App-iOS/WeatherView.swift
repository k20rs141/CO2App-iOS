//
//  WeatherView.swift
//

import SwiftUI

struct WeatherView: View {
    var body: some View {
        VStack {
            Text("weather view")
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(.ultraThinMaterial)
    }
}

#Preview {
    WeatherView()
}
